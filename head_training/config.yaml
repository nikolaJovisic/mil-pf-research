logs_path: mammo_filter/head_training/logs

input_dim: 384
model_type: dinout 
hidden_dim: #always in grid search

full_state: False 
pooler: #

#velo
#no specifics

#dinout
double_input_layer: #

#baseline
use_tiles: false
aggregation: max

#perceiver
use_shared_projector: true
fusion_mode: linear
layer_norm: false
prehidden_dim: 32

#probably not going to edit much
pos_weight: 1.0
freeze_whole_image_branch: false
flatten: false

#training
batch_size: 2e16 #4096 
lr: 0.02
weight_decay: 1e-4
epochs: 500
patience: 20

#saving
eval_every: 30
load_path:
save_path: 
