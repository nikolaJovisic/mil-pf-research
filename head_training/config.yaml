logs_path: /home/nikola.jovisic.ivi/nj/mammo_filter/logs

valid_size: 0.15
test_size: 0.15
pos_weight: 1.5
use_tiles: true

input_dim: 768
hidden_dim: 64
aggregation: attention
flatten: false

freeze_whole_image_branch: true

batch_size: 512
lr: 0.02
weight_decay: 1e-4
epochs: 300
patience: 20

eval_every: 30
load_path: pretrain.pt
save_path: trained.pt